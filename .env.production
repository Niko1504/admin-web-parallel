# Use the same-origin Vercel proxy (see vercel.json rewrites: /api/* -> https://api.washio.app/api/*).
# A direct cross-origin URL here makes the browser block API reads — the backend does not
# send Access-Control-Allow-Origin for the admin origin, which broke the Users page (and all data loads).
VITE_API_URL=/api
